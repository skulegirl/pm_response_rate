A module that provides information on response rate for private messages sent to a user.

Provides the rate as a percentage based on configurable factors (e.g. only consider messages in last x days, etc)
Also provides the response time, e.g. within a few hours, a few days, a week, etc.
